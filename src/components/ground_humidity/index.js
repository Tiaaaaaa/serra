import React, { Component } from "react";
import { render } from "react-dom";
import { Window, WindowTitle } from '../../common/style';

export const GroundHumidity = () => {
  return (
    <Window>
      <WindowTitle>Umidità della terra</WindowTitle>
    </Window>
  );
};
